import { StyleSheet, Text, TouchableOpacity, View } from 'react-native';

const StripePayment = () => {
  return (
    <View>
      <Text>Successfully navigate to Stripe</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default StripePayment;
